import { NumerosApenasDirective } from './numeros-apenas.directive';

describe('NumerosApenasDirective', () => {
  it('should create an instance', () => {
    const directive = new NumerosApenasDirective();
    expect(directive).toBeTruthy();
  });
});
