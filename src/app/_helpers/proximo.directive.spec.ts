// tslint:disable-next-line: ordered-imports
import { ProximoDirective } from './proximo.directive';

describe('Proximo.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ProximoDirective();
    expect(directive).toBeTruthy();
  });
});
